# Valdi Scan-Type Validation

- **Timestamp:** 2026-04-07T10:00:00Z
- **Scan type:** Nmap port scanning and service detection
- **Scan type ID:** nmap_port_scan
- **Declared Layer:** 2 (Active Probing)
- **Declared Level:** 1 (Written consent required)
- **Verdict:** APPROVED
- **Approval token:** 18978d41-4d3b-40d0-8b42-018373270759
- **Triggered by:** Federico (Gate 1 validation request)

## Function Reviewed

```python
def _run_nmap(domains: list[str]) -> dict[str, dict]:
    """Layer 2 / Level 1 — Port scanning and service detection via Nmap.

    Scans top-N TCP ports plus critical infrastructure supplement ports with
    service version detection. Requires written consent (Level 1) before
    execution.

    Returns ``{domain: {"open_ports": [...], "port_count": N}}``.
    """
    if not shutil.which("nmap"):
        logger.warning("nmap not found in PATH — skipping port scan")
        return {}

    results: dict[str, dict] = {}

    for domain in domains:
        if not _DOMAIN_RE.match(domain):
            logger.warning("nmap: invalid domain format {!r} — skipping", domain)
            continue

        try:
            proc = subprocess.run(
                [
                    "nmap",
                    "-sV",
                    "-Pn",
                    "-T3",
                    "--top-ports", str(NMAP_TOP_PORTS),
                    "-p", NMAP_SUPPLEMENT_PORTS,
                    "--open",
                    "--max-retries", "2",
                    "--host-timeout", "300",
                    "--defeat-rst-ratelimit",
                    "-oX", "-",
                    domain,
                ],
                capture_output=True,
                text=True,
                timeout=NMAP_TIMEOUT,
            )

            if proc.returncode != 0 and proc.stderr:
                logger.warning("nmap exited with code {} for {}: {}",
                               proc.returncode, domain, proc.stderr[:500])

            parsed = _parse_nmap_xml(proc.stdout, domain)
            if parsed["port_count"] > 0:
                results[domain] = parsed

            logger.bind(context={
                "domain": domain,
                "open_ports": parsed["port_count"],
            }).info("nmap_complete")

        except subprocess.TimeoutExpired:
            logger.warning("nmap timed out after {}s for {}", NMAP_TIMEOUT, domain)
        except FileNotFoundError:
            logger.warning("nmap binary not found")
            return results

    logger.info("nmap: scanned {}/{} domains, {} with open ports",
                len(domains), len(domains), len(results))
    return results
```

### Helper Function: `_parse_nmap_xml`

```python
def _parse_nmap_xml(xml_output: str, domain: str) -> dict:
    """Parse nmap XML output for a single host into structured port data."""
    try:
        root = ET.fromstring(xml_output)
    except ET.ParseError as exc:
        logger.warning("nmap: XML parse error for {}: {}", domain, exc)
        return {"open_ports": [], "port_count": 0}

    open_ports: list[dict] = []

    for host in root.findall("host"):
        ports_elem = host.find("ports")
        if ports_elem is None:
            continue
        for port_elem in ports_elem.findall("port"):
            state_elem = port_elem.find("state")
            if state_elem is None or state_elem.get("state") != "open":
                continue

            service_elem = port_elem.find("service")
            open_ports.append({
                "port": int(port_elem.get("portid", "0")),
                "protocol": port_elem.get("protocol", "tcp"),
                "state": "open",
                "service": service_elem.get("name", "") if service_elem is not None else "",
                "product": service_elem.get("product", "") if service_elem is not None else "",
                "version": service_elem.get("version", "") if service_elem is not None else "",
            })

    return {"open_ports": open_ports, "port_count": len(open_ports)}
```

## Tools Invoked

- nmap (Layer 2 -- written consent required, listed in SCANNING_RULES.md Layer 2 tools table)

## Nmap Flags Analysed

| Flag | Purpose | Layer 3 Risk |
|------|---------|-------------|
| `-sV` | Service version detection | No -- detection only, no exploitation |
| `-Pn` | Skip host discovery, treat all hosts as up | No -- reduces probing (skips ping sweep) |
| `-T3` | Normal timing template | No -- standard rate, not aggressive |
| `--top-ports 100` | Scan top 100 TCP ports by frequency | No -- port scanning is Layer 2, properly gated |
| `-p <supplement>` | 13 additional infrastructure ports | No -- same as above, port scanning |
| `--open` | Only report open ports | No -- output filter |
| `--max-retries 2` | Limit retransmission attempts | No -- reduces scan intensity |
| `--host-timeout 300` | 5-minute per-host timeout | No -- prevents runaway scans |
| `--defeat-rst-ratelimit` | Adjust timing for RST-rate-limited hosts | No -- timing adjustment, not exploitation |
| `-oX -` | XML output to stdout | No -- output format |

## Flags Confirmed Absent

- **`-sC` (default scripts):** NOT present. No NSE script execution.
- **`--script`:** NOT present. No custom or category-based NSE scripts.
- **`-A` (aggressive scan):** NOT present. Would enable OS detection + scripts.
- **`-O` (OS detection):** NOT present.
- **`--vuln` (vulnerability scripts):** NOT present.
- **Any exploitation flags:** None found.

## URLs/Paths Requested

- TCP ports on target host -- port scanning is directed probing (Layer 2), correctly requires consent
- No HTTP paths, no URL construction -- this function probes network ports, not web paths

## robots.txt Handling

The function does not check robots.txt internally. This is consistent with the other Layer 2 functions (`_run_nuclei`, `_run_cmseek`) which also do not perform internal robots.txt checks. For Layer 2 (consent-gated) scan types, robots.txt compliance is enforced at Gate 2 (per-target authorisation) before the function is called. This is an acceptable architecture -- the upstream orchestrator and consent validator handle robots.txt denial before any Layer 2 function receives a domain.

## Input Validation

- Domain format validated via `_DOMAIN_RE = re.compile(r"^[a-zA-Z0-9._-]+$")` before passing to subprocess -- prevents command injection
- Timeout enforced at `NMAP_TIMEOUT = 120` seconds via `subprocess.run(timeout=...)`
- Tool availability checked via `shutil.which("nmap")` before execution

## Consent Gating

- Function is registered in `_LEVEL1_SCAN_FUNCTIONS` (line 1060), which requires written consent
- Docstring explicitly declares "Layer 2 / Level 1 -- Requires written consent (Level 1) before execution"
- The `_validate_approval_tokens(max_level=1)` check must pass before Level 1 functions execute

## Reasoning

The `_run_nmap` function is correctly classified as Layer 2 (Active Probing). Nmap is explicitly listed in SCANNING_RULES.md as a Layer 2 tool requiring written consent. The function:

1. **Uses only detection flags** -- `-sV` performs service version detection via banner grabbing. No NSE scripts (`-sC`, `--script`) are invoked. No exploitation occurs.
2. **Is properly consent-gated** -- registered in `_LEVEL1_SCAN_FUNCTIONS`, ensuring it only executes for targets with written authorisation.
3. **Contains no Layer 3 activity** -- no exploitation, no script execution, no authentication attempts, no vulnerability exploitation.
4. **Has appropriate safeguards** -- domain format validation (prevents injection), per-host timeouts, retry limits, and graceful error handling.
5. **`--defeat-rst-ratelimit`** -- this flag adjusts nmap's timing when a target rate-limits TCP RST responses. It is a timing optimisation, not an exploitation technique. It prevents false negatives from dropped probes. It does not bypass security controls or perform any exploitation.
6. **Supplement ports** -- the 13 additional ports (Docker API, Redis, Elasticsearch, MongoDB, etc.) are critical infrastructure services that should never be internet-facing on SMB targets. Scanning these is within the scope of a consented security assessment.

All flags are detection-only. The function complies with SCANNING_RULES.md at the declared Layer 2 / Level 1.

## Violations

None.
