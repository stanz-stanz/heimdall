# Valdi Scan-Type Validation

- **Timestamp:** 2026-03-22T10:10:00Z
- **Scan type:** robots.txt compliance check
- **Scan type ID:** robots_txt_check
- **Declared Layer:** 1 (Passive)
- **Declared Level:** 0 (No consent)
- **Verdict:** APPROVED
- **Approval token:** db78dd3c-a221-4b25-a57f-1779b31d66e3
- **Function hash:** sha256:4177e71d888b6ac59423f23a6e8b0a57177b44e0cca5f315c6c2b47783c4826b
- **Triggered by:** Claude Code (new function — created to resolve scan_domains rejection)

## Function Reviewed

```python
def _check_robots_txt(domain: str) -> bool:
    """Layer 1 / Level 0 — Check if robots.txt allows automated access.

    Returns True if access is allowed, False if denied.
    Fetching robots.txt is permitted at all Layers — it is an explicitly published file.
    """
    try:
        resp = requests.get(
            f"https://{domain}/robots.txt",
            timeout=REQUEST_TIMEOUT,
            headers={"User-Agent": USER_AGENT},
            allow_redirects=True,
        )
        if resp.status_code != 200:
            return True  # No robots.txt — no restriction expressed
        rp = RobotFileParser()
        rp.parse(resp.text.splitlines())
        return rp.can_fetch("*", "/")
    except requests.RequestException:
        return True  # Cannot fetch robots.txt — no restriction determinable
```

**File:** `pipeline/scanner.py` (lines 239-258)

## Tools Invoked

- Python `requests` library (HTTP GET)
- Python `urllib.robotparser.RobotFileParser` (stdlib parser)

## URLs/Paths Requested

- `https://{domain}/robots.txt` — explicitly published file, permitted at all Layers

## robots.txt Handling

This function IS the robots.txt handler. It fetches and parses robots.txt to determine whether automated access is permitted for the root path. Its return value gates all subsequent scanning activity in the orchestrator.

Default behaviour:
- 200 response with robots.txt content: parse and check `Disallow` rules for `User-agent: *`
- Non-200 response (404, 403, etc.): return True (no restriction expressed)
- Network error: return True (cannot determine restriction)

## Reasoning

SCANNING_RULES.md Level 0 explicitly allows: "robots.txt and security.txt — these are explicitly published for public consumption."

This function fetches a single explicitly published file (`/robots.txt`) and parses its contents using Python's standard library parser. It does not probe hidden paths, send crafted requests, or perform any vulnerability testing. The function exists solely to enforce the mandatory constraint: "If a target's robots.txt denies automated access, skip the target entirely."

The function:
1. Only accesses `/robots.txt` — an explicitly published file
2. Uses a standard GET request with standard User-Agent
3. Does NOT probe any other paths
4. Does NOT send crafted requests
5. Serves a compliance-positive purpose (enforcing robots.txt respect)

All activity is within Layer 1. No Layer 2 or Layer 3 activity present.

## Violations

None.
